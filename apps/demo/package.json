{
  "name": "@inboxlink/demo",
  "version": "0.0.1",
  "private": true,
  "description": "Tiny host-app demo that calls @inboxlink/sdk",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@inboxlink/sdk": "workspace:*"
  },
  "engines": {
    "node": ">=22"
  }
}
