# @inboxlink/connect-ui (stub)

Hosted **Connect inbox** UI (Plaid Link analog).

v0 serves a minimal HTML connect page from `@inboxlink/server` at `/v1/connect/:linkToken`.
This package is a placeholder for a future React (or plain HTML) widget.

Do not depend on career-workspace.
